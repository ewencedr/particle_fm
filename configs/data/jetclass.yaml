_target_: src.data.jetclass_datamodule.JetClassDataModule

# general parameters
batch_size: 1024
num_workers: 32
pin_memory: False
val_fraction: 0.10
test_fraction: 0.30

# preprocessing
normalize: True
normalize_sigma: 5

# spectator_jet_features:
#   - jet_pt

# select jet types to use
# list of the following: QCD, Hbb, Hcc, Hgg, H4q, Hqql, Zqq, Wqq, Tbqq, Tbl
used_jet_types: null # null means all jet types
additional_part_features: null # null means no additional part features

# files and jet types to use
data_dir: /beegfs/desy/user/birkjosc/datasets/jetclass_h5_all_features_5M_training_jets/
filename_dict:
  train: ${data.data_dir}/train_100M/merged_standardized.h5
  val: ${data.data_dir}/val_5M/merged_standardized.h5
  test: ${data.data_dir}/test_20M/merged_standardized.h5
# the file below contains the masks and m/pt/nparticles generated by another model
# --> use for generating new point clouds
conditioning_gen_filename: /home/birkjosc/repositories/flow-matching-studies/jet_feature_generation/trained_models/flow_2hidden_layers_256hidden_features_200_000steps_0.99998gamma_500_000jets.h5
