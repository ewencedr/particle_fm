# @package _global_

defaults:
  - _self_
  - data: /jetnet/jetnet_tops30.yaml # choose datamodule with `test_dataloader()` for evaluation
  - model: cnf_flow_matching.yaml
  - logger: many_loggers.yaml
  - trainer: gpu.yaml
  - paths: default.yaml
  - extras: default.yaml
  - hydra: default.yaml

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: ???
