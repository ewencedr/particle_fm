# https://www.comet.ml

comet:
  _target_: pytorch_lightning.loggers.comet.CometLogger
  api_key: os.environ.get("COMET_API_KEY"), # api key is loaded from environment variable
  save_dir: "${paths.output_dir}"
  project_name: "deep-learning"
  rest_api_key: null
  # experiment_name: ""
  experiment_key: null # set to resume experiment
  offline: False
  prefix: ""
